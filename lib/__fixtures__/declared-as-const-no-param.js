import { TouchableNativeFeedback, Text } from 'react-native';
import React, { Component, PropTypes } from 'react';

const Button = () => <Text style={{ color: 'blue' }}>Test</Text>;

export default Button;
